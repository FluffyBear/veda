// Editor Presenter

veda.Module(function EditorPresenter(veda) { "use strict";
/*  var template = $("#editor-template").html();
  // Initialize editor page
  veda.on("load:editor", function (container_param) {
    if (container_param === null) return;
    var container = $(container_param || "#main");
    container.empty().append(template);
    // container.append($.fn.veda_link);
    container.show();
  });*/
});